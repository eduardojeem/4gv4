/**
 * Shared constants for the public products section.
 * Used by page.tsx, components.tsx, ProductFilters.tsx, and products-server.ts.
 */

/** Upper bound for the price filter slider. Treated as "no max" when selected. */
export const PRODUCTS_MAX_PRICE = 50_000_000
